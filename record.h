#ifndef _RECORD_H
#define _RECORD_H

#include <vector>
#include <string>

class Record {
public:
	vector<string> stringVector; // this could be a different container
	void access_entry(string entry);
private:

};

#endif
